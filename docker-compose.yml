services:
  web:
    build: .
    ports:
      - "${APP_PORT}:35000"
    environment:
      - SERVER_BOT_TOKEN
      - CHANNEL_ID
      - APP_PORT
      - REDIS_HOST
      - REDIS_PORT

  redis:
    image: redis:alpine
